<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>adddorm</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        .contain {
            background-color: #CABA9C;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .main {
            margin: 20px;
            display: grid;
            grid-template-columns: 1fr 0fr;
            gap: 20px;
            padding: 40px;
            background: #ffffff;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            width: 80%;
            max-width: 900px;
        }

        .text-information {
            display: flex;
            flex-direction: column;
            gap: 15px;
            font-size: 18px;
            padding: 20px;
        }

        .text-information label {
            font-weight: bold;
            margin-bottom: 5px;
        }

        .text-information input {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 5px;
            outline: none;
            transition: border-color 0.3s ease-in-out;
        }

        .text-information input:focus {
            border-color: #8A6240;
        }

        .bt-save {
            background-color: #8A6240;
            color: white;
            padding: 12px;
            font-size: 18px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: underline;
            margin-top: 15px;
        }

        .bt-save:hover {
            background-color: #6d4f34;
            transform: scale(1.05);
        }

        .image-information {
            background: #f9f9f9;
            border-radius: 10px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
            min-height: 400px;
            border: 2px dashed #8A6240;
        }

        .image-information img {
            max-width: 100%;
            height: auto;
            border-radius: 10px;
            object-fit: cover;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .upload {
            padding: 10px 20px;
            font-size: 16px;
            color: #fff;
            background-color: #d4a073;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .upload:hover {
            background-color: #f0d6bf;
        }
        .btback {
            position: absolute;
            bottom: 10%;
            right: 10%;
        }
        .btback {
            position: fixed;
            bottom: 30px;
            right: 40px;
            background-color: #8A6240;
            color: white;
            padding: 12px 20px;
            font-size: 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s ease-in-out;
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="contain">
        <div class="main">
            <form action="/adddorm/process_addorm" method="POST" enctype="multipart/form-data">
                <div class="text-information">
                    <label for="">ชื่อหอพัก :</label>
                    <input type="text" name="name" required>
                    <label for="">รายละเอียด :</label>
                    <input type="text" name="detail" required>
                    <label for="">ที่อยู่ :</label>
                    <input type="text" name="address" required>
                    <label for="">จำนวนห้อง :</label>
                    <input type="text" name="room" required>
                    <label for="">ช่วงราคาห้องพัก :</label>
                    <input type="text" name="rent" placeholder="เช่น x,xxx - x,xxx" required>
                    <label for="">เบอร์ติดต่อ :</label>
                    <input type="text" name="contact" required>
                    <label for="">ช่องทางติดต่ออื่น :</label>
                    <input type="text" name="other_contact">
                    <label for="">ค่าไฟ :</label>
                    <input type="text" name="electric_pay" required>
                    <label for="">ค่าน้ำ :</label>
                    <input type="text" name="water_pay" required>
                    <div class="image-information">
                        <div class="upload-icon" id="uploadIcon"></div>
                        <img src="" alt="Image Here" id="imagePreview" for="finp">
                    </div>
                    <input type="file" name="image" required style="display: none;" id="fileInput" class="finp">
                    <input type="button" value="อัพโหลดรูปภาพ" class="upload" onclick="document.getElementById('fileInput').click();">
                    <button class="bt-save" type="submit">บันทึก</button>
                </div>
            </form>
        </div>
    </div>
    <button class="btback" onclick="window.location.href='/owneddorm';">ย้อนกลับ</button>
    <script>
        const fileInput = document.getElementById('fileInput');
        const imagePreview = document.getElementById('imagePreview');

        fileInput.addEventListener('change', function () {
            const file = this.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function (e) {
                    imagePreview.src = e.target.result;
                    imagePreview.style.display = 'block';
                };
                reader.readAsDataURL(file);
            }
        });
    </script>
</body>

</html>
