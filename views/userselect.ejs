<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>selectinfo</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        main {
            background-color: #ffffff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        button {
            display: block;
            justify-content: center;
            align-items: center;
            margin: 110px;
            background-color: #7F5432;
            width: 1200px;
            height: 140px;
            border-radius: 100px;
            font-size: 57px;
            color: #ffffff;
            border: none;
            box-shadow: black 100px solid;
            cursor: pointer;
            box-shadow: 25px 25px 25px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease;
        }

        button:hover {
            box-shadow: 6px 6px 15px rgba(0, 0, 0, 0.3);
            /* Stronger shadow on hover */
        }
    </style>
</head>

<body>
    <main>
        <div>
            <button id="dorm-info">รายละเอียดหอพัก</button>
            <button id="report-form">แจ้งปัญหา</button>
        </div>
    </main>
</body>

</html>