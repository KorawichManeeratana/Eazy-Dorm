<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Success</title>
    <style>
        html, body {
            height: 100%;
            margin: 0;
            overflow: hidden;
        }

        #wrapper {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
            background-color: #f0f0f0;
        }
        .container {
            text-align: center;
            background-color: white;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
            width: 80%;
            max-width: 600px;
        }
        .icon {
            font-size: 120px;
            color: green;
        }
        .message {
            font-size: 48px;
            margin-top: 30px;
            color: green;
        }
        .home-button {
            display: inline-block;
            padding: 15px 30px;
            background-color: #4CAF50;
            color: white;
            text-decoration: none;
            border-radius: 8px;
            margin-top: 40px;
            font-size: 24px;
            transition: background-color 0.3s ease;
        }
        .home-button:hover {
            background-color: #367c39;
        }
    </style>
</head>
<body>
    <div id="wrapper">
        <div class="container">
            <div class="icon">✔️</div>
            <div class="message">ชำระเงินเสร็จสิ้น</div>
            <a href="/" class="home-button">กลับหน้าหลัก</a>
        </div>
    </div>
</body>
</html>